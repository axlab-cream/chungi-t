# 운명상회 Tone V2 pass_angle 52항목 전환

## 결과

명시적 `긴 목차 규칙` 블록만 파싱해 `pass_angle`을 10개 그룹·52개 항목으로 연결했다. 다섯 원천 파일의 분할은 10/11/10/10/11이며 파일 해시와 제목·순서를 테스트에 고정했다. 예시 출력 본문은 코퍼스나 고객 문장으로 재사용하지 않았다.

실제 provider 합성 실행은 첫 항목이 기존 2회 재시도 뒤 품질 검수에 실패했고, 이후 51항목 호출은 0건이었다. 따라서 전체 실제 출력 승인은 실패로 유지한다.

## 재사용 규칙

- 긴 리포트는 모든 완료 본문을 다음 프롬프트에 누적하지 않는다. 모든 항목의 ID·질문·짧은 훅은 유지하고, 원문은 최근 4개·각 1,200자로 제한한다.
- 전체 형제 본문은 생성 프롬프트가 아니라 결정적 사후 검수와 저장 replay에만 사용한다.
- 전체 실행은 고유 버전 사전 조회, 신규 레코드, 앞에서부터 순차 처리, 최초 실패 즉시 중단, 실패 뒤 호출 0건을 증거로 남긴다.
- 전체 성공을 주장하려면 52/52 완료와 저장 replay 전수 PASS가 모두 필요하다.

## 현재 결함

첫 `전체 흐름 판정` 출력은 두 번째 시도에서도 상징 해석 경계, 구체 장면, 다음 판단 기준, 한자 설명 분리 규칙을 동시에 통과하지 못했다. 다음 Task는 이 첫 항목의 생성 안내와 검수 불일치만 좁게 다룬다. 같은 실행 버전은 재사용하지 않는다.

## 증거

- `tone-v2/evaluations/P04-pass-angle-full-outline-20260912.json`
- `CreamAI/logs/review/task-tone-v2-p04-pass-angle-full-outline_implementation-review.md`
- focused 53/53, full 681/681, compiler/task 7/7, typecheck/build/diff PASS

운영 DB·고객 데이터·결제·인증·Production·배포는 사용하거나 변경하지 않았다. API 키·원문 응답은 이 문서에 저장하지 않았다.
