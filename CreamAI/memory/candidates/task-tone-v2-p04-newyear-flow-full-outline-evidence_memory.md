# Memory candidate — newyear_flow full-outline evidence

For long GPT-5 report prompts that exhaust `max_completion_tokens` on reasoning before emitting JSON, keep the normal Production token budget and use an explicit isolated-test-only low reasoning effort. Preserve the first-failure barrier, then resume only the unresolved section; never regenerate accepted predecessors. Track source hashes, record/prose hashes, attempts and aggregate review metrics, but do not commit provider prose or credentials.
