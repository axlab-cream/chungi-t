# Approved memory — newyear_flow full-outline evidence

Verified pattern: freeze the source outline and hashes, create one unique isolated synthetic record, use a test-only low reasoning effort when long GPT-5 prompts consume the output budget, stop at the first unresolved item, and resume only that item. Accept only production-equivalent stored-snapshot replay. Persist hashes and aggregate metrics without provider prose, secrets, or personal data. This produced 36/36 accepted and replay-passing sections without Production/customer mutation.
