# KMS note — Lucky Color actual-result visual acceptance

- Reuse the immutable synthetic generation record through the production saved-result reader; do not validate a sample fallback or mock component.
- A long native-disclosure report can be accepted with one exact 390px fixed-width frame, provided all disclosure bodies are also opened and checked for overflow and empty content.
- Preserve the existing black/gold reader system when the hierarchy, targets and long-form reflow already pass; visual verification is not a redesign task.
- Print acceptance requires every saved-result disclosure to be expanded before printing and prior screen state restored afterward.
- Validate print structurally for every section and visually for representative first, middle and final pages.
- Keep screenshots and PDFs ignored locally. Track only counts, findings and SHA-256 hashes; never copy provider prose into release evidence.

