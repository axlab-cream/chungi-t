# KMS: Pass Angle real-reader visual acceptance

- Use one immutable complete stored result and a loopback-only read-only QA server.
- Verify section identity/count/content separately from viewport presentation.
- Measure actual rendered control boxes; CSS minimums can render smaller under an inherited scaled shell.
- Inspect a complete print contact sheet plus representative full-size pages; direct introductory nodes need explicit print colors.
- Preserve customer and Production state; store tracked evidence as sanitized hashes/counts/findings only.
