# 직장 선택 공용 리더 모바일 배포 검증 — 2026-09-22

- 오류: 공용 대운 타임라인의 내부 최소 너비가 모바일 리포트의 상위 그리드에 전파되어 요약 이미지와 본문이 오른쪽으로 잘렸다.
- 수정: 해석 슬롯과 longform/summary 그리드에 `minmax(0,1fr)` 및 `min-width:0`을 적용하고 정적 CSS 버전을 갱신했다. 타임라인은 카드 내부에서만 가로 스크롤한다.
- 검증: 로컬 430px/360px 렌더링의 외부 가로 넘침 없음, 집중 테스트 79/79, TypeScript, production build, GitHub CI 성공. 운영 브라우저에서 요약 이미지와 토글 내부 이미지, 펼침·접힘을 직접 확인했다. 운영 API health 및 정적 요약 이미지 HTTP 200.
- 운영: `origin/main` 커밋 `2026111403ce2db4d7114c066fdb37cb7ed4ba54`, Vercel 배포 `dpl_WnSnW9aCpN5ai1T4RhBs3MxCqDcx` Ready. 팝업/팝업 관리자 파일 변경 없음.
- 재사용 원칙: 내부 수평 스크롤 컴포넌트를 CSS grid 자식에 넣을 때 상위 그리드의 최소 콘텐츠 폭을 명시적으로 0으로 제한하고, 운영 브라우저에서 실제 사용자 폭을 검증한다.
