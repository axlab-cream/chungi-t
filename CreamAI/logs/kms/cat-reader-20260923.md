# 고양이 궁합 저장 해석의 공용 렌더러 이미지·토글 운영 적용

- 문제: 고양이 궁합 저장 리포트가 공통 요약 사진 한 장만 사용하고 20개 해석 토글에 맥락별 이미지가 없었다. 완료된 저장 해석 아래에 낡은 결제 확인 안내가 노출될 수 있었다.
- 결정: 저장된 본문과 회원별 계산 데이터는 변경하지 않고, `longform-blocks.json`의 서비스별 이미지 계약에 요약·하이라이트·20개 개별 사진을 연결했다. 공용 렌더러를 유지하며 최초 설정 로딩 순서를 보장했다. 고양이 리포트 전용 CSS로 본문 스크롤, 사진 비율, 15px/1.9 글 가독성, 펼치기/접기 상태를 명확히 했다.
- 구현: `a0295eb`를 `main`에 일반 푸시. 운영 Vercel `dpl_9Tez27zsNQj7PtNV11cydTd7wm4T` Ready, `umsh.kr` alias 확인. 22개 사진은 각각 단일 장면의 실사이며 총 1.2MB 수준이다.
- 검증: 전체 단위 테스트 1539/1539, 20서비스 QA 20/20, typecheck/build 성공. 로그인된 운영 리포트에서 20개 토글·20개 서로 다른 카드 이미지·요약/하이라이트 이미지를 확인했다. 마지막 카드 펼치기·접기와 사진 로딩, 구형 결제 안내 숨김, 본문 전체 스크롤, 하단 공유/PDF 좌우 배치 확인. 운영 이미지 22/22 HEAD 200.
- 범위: 팝업/관리자, 인증, 결제, DB 스키마, 기존 저장 해석 본문은 수정하지 않았다. 이미지와 서비스 전용 표시가 공용 렌더러의 계약을 통해 회원별 저장 해석에 적용된다.
- 교훈: 카드별 사진 경로를 HTML 복제 대신 서비스 설정에 두고, 첫 렌더보다 설정 로딩을 앞세워야 첫 방문에도 새 사진이 표시된다. 배포 후에는 빌드 성공만 보지 말고 로그인된 저장 리포트의 실제 이미지 `naturalWidth`와 토글 상태를 확인한다.
