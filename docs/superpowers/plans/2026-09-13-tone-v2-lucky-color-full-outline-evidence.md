# Tone V2 lucky_color full-outline evidence plan

Task: `task-tone-v2-p04-lucky-color-full-outline-evidence`

## Objective

Validate the supplied three-part `lucky_color` outline, attach it to the real generation pipeline and produce one fresh isolated provider-backed report with immutable provenance and independent quality review.

## Execution

1. Freeze exact source hashes, headings, order and item count without copying supplied prose.
2. Add RED tests for source contract, runtime outline identity, fresh pending storage, isolation and first-failure stop behavior.
3. Implement only the smallest missing runtime/harness support required for the exact outline.
4. Run the provider in a unique synthetic owner/version record, preserve every attempt and stop at the first unresolved section.
5. Replay every accepted section through the production-equivalent grounding, persona, tone, duplication, safety and continuity review.
6. Record provider/model metadata, token/attempt counts and content hashes without credentials, personal data or customer records.
7. Run focused, related, full, typecheck/build, review, credential and ProjectOps gates.

## Non-scope

- Production attachment, deployment or customer-record mutation.
- Other services, payment, auth, admin or database changes.
- Commit or push.

## Acceptance

- Exact source structure is deterministic and no sample prose is reused.
- All outline items complete and pass replay, or execution fails closed at the first unresolved item with no later calls.
- The generation is isolated, immutable, independently reviewed and safe to use only as release evidence.
